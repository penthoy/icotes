# Production Environment Configuration
# This file is used during production builds

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_URL=http://localhost:8000

# Port Configuration Notes:
# - For Coolify: The PORT environment variable will be automatically set and will override BACKEND_PORT
# - For manual deployment: Set BACKEND_PORT to your desired port
# - For containerized deployment: Use PORT environment variable (standard practice)

# Frontend Configuration  
FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=3000
FRONTEND_URL=http://localhost:3000

# API Configuration (used by frontend)
VITE_API_URL=/api
VITE_WS_URL=

# Environment
NODE_ENV=production

# SSL Configuration (if using HTTPS)
# SSL_CERT_PATH=/path/to/your/cert.pem
# SSL_KEY_PATH=/path/to/your/key.pem
