# Production Environment Configuration
# This file is used during production builds

# Site Configuration
SITE_URL=localhost
PORT=8000

# Single port configuration - everything runs on port 8000
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_URL=http://localhost:8000

# Frontend should also be served from port 8000 in single port mode
FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=8000
FRONTEND_URL=http://localhost:8000

# Vite-accessible environment variables (prefixed with VITE_)
VITE_BACKEND_URL=http://localhost:8000
VITE_API_URL=http://localhost:8000/api
VITE_SITE_URL=localhost
VITE_WS_URL=ws://localhost:8000/ws

# Environment
# NODE_ENV=production (commented out - not supported in .env files by Vite)

# Workspace Configuration
WORKSPACE_ROOT={PROJECT_ROOT}/workspace
VITE_WORKSPACE_ROOT={PROJECT_ROOT}/workspace

# Port Configuration Notes:
# - For Coolify: The PORT environment variable will be automatically set and will override BACKEND_PORT
# - For manual deployment: Set BACKEND_PORT to your desired port
# - For containerized deployment: Use PORT environment variable (standard practice)

# SSL Configuration (if using HTTPS)
# SSL_CERT_PATH=/path/to/your/cert.pem
# SSL_KEY_PATH=/path/to/your/key.pem

# API Keys for Agents (set these in your production environment):
# OPENAI_API_KEY=your_openai_api_key_here
# OPENROUTER_API_KEY=your_openrouter_api_key_here
# GROQ_API_KEY=your_groq_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# CEREBRAS_API_KEY=your_cerebras_api_key_here
# PUSHOVER_USER=your_pushover_user_here
# PUSHOVER_TOKEN=your_pushover_token_here
# MAILERSEND_API_KEY=your_mailersend_api_key_here
# TAVILY_API_KEY=your_tavily_api_key_here
# SERPER_API_KEY=your_serper_api_key_here
# DASHSCOPE_API_KEY=your_dashscope_api_key_here
# MOONSHOT_API_KEY=your_moonshot_api_key_here

# Agent runtime limits (for long-running tasks)
# Max tokens per response chunk; handler will auto-continue if truncated
AGENT_MAX_TOKENS=8000
# Enable auto-continue when a response is cut off due to token limits (1=true, 0=false)
AGENT_AUTO_CONTINUE=1
# Maximum number of continuation rounds to resume output
AGENT_MAX_CONTINUE_ROUNDS=10

# Gemini-specific features
# Enable thought signature preservation for multi-step task completion (on/off, default: on)
GEMINI_THOUGHT_SIGNATURES=on
